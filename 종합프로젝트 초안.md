# 외식 창업 기획 도우미 (가칭)

## 프로젝트 개요

아무것도 정하지 않은 예비 외식 창업자에게 **업종 선택 → 입지 추천 → 컨셉·메뉴 → 브랜드명·로고**까지 한 번에 설계해주는 서비스.
공공데이터로 상권을 분석하고, 그 결과를 근거로 LLM이 기획안과 브랜딩을 생성한다.

- 대상 지역: 전국
- 대상 업종: 음식점(술집 포함), 카페
- 형태: 대학교 종합 프로젝트(팀). 현재는 1인이 전체 흐름을 먼저 만드는 단계

### 파이프라인 (앞 단계 출력이 다음 단계 입력)

```
사용자 조건(예산·희망 지역·선호 업종·경험)
  → 입지·업종 추천 (행정동 × 업종 지표 기반 점수)
  → 컨셉·메뉴·가격대 (LLM)
  → 브랜드명·로고 (LLM + 이미지/SVG)
  → 결과 화면 (지도·업종 분포도·기획안)
```

단계 사이의 입출력은 `docs/interfaces.md`에 JSON 형식으로 정의하고, 각 단계는 그 형식만 주고받는다.
(나중에 팀원에게 단계 단위로 분담하기 위함)

## 현재 단계

**0단계: 프로젝트 초기 세팅 + 데이터 EDA.** 분석·모델·API 코드는 아직 만들지 않는다.

## 폴더 구조

```
project/
├─ CLAUDE.md
├─ README.md
├─ requirements.txt
├─ .gitignore
├─ data/
│  ├─ raw/         # 원본 다운로드 파일. 절대 수정·삭제하지 않음
│  ├─ interim/     # 인코딩 정리·parquet 변환 등 중간 산출물
│  └─ processed/   # 행정동 × 업종 집계표 등 최종 분석용 데이터
├─ notebooks/      # 탐색용 노트북
├─ src/
│  └─ eda/         # EDA 스크립트
├─ reports/        # eda.md 등 분석 결과 문서
└─ docs/           # interfaces.md 등 설계 문서
```

## 데이터

모두 공공데이터포털(data.go.kr) 파일데이터. 사용자가 직접 받아 `data/raw/`에 넣는다.

| 이름 | 용도 | 비고 |
|---|---|---|
| 소상공인시장진흥공단_상가(상권)정보 | 점포 위치·업종 분포·동종 밀집도 | 영업 중인 점포만 포함(폐업 분석 불가). 시도별 CSV. 업종 대10·중75·소247. 위경도 제공 |
| 행정안전부_일반음식점 | 한식·중식·술집 등 개업·폐업 이력 | 인허가일자·폐업일자·영업상태 포함. 좌표는 TM(EPSG:5174로 안내됨) |
| 행정안전부_휴게음식점 | 카페 개업·폐업 이력 | 위와 같은 구조로 추정. 확인 필요 |
| 행정안전부_지역별(행정동) 성별 연령별 주민등록 인구수 | 배후 인구·연령 구성 | 행정동 단위 |

아직 없는 것: 행정동 경계 파일(공간 조인이 필요하다고 판단되면 그때 받음), 매출·유동인구(전국 원천 파일 확보 불확실 → 보조 지표로만 고려).

## 작업 규칙

- 환경은 **Windows**. 경로는 `pathlib` 사용, 파일 입출력은 인코딩을 항상 명시(`utf-8` 또는 `cp949`). 콘솔 출력 한글 깨짐에 주의
- `data/raw/`는 읽기 전용으로 취급한다
- `data/` 전체는 `.gitignore`에 넣는다 (대용량·재다운로드 가능)
- 대용량 CSV는 필요한 컬럼만 `usecols`로 읽거나 `chunksize`로 처리. 한 번 읽은 원본은 `data/interim/`에 parquet로 저장해 재사용
- 인코딩을 모르면 `cp949` → `utf-8-sig` → `utf-8` 순으로 시도하고, 성공한 인코딩을 보고서에 기록
- 추측으로 채우지 말 것. 컬럼 의미·좌표계·코드 체계가 불확실하면 보고서에 "확인 필요"로 남긴다
- 스크립트는 다시 실행해도 같은 결과가 나오게 작성

## 사용 스택

Python 3.11+, pandas, pyarrow, pyproj, openpyxl, chardet. (geopandas는 공간 조인 단계에서 추가)

---

## 작업 1: 초기 세팅

1. 위 폴더 구조 생성 (`data/raw/`에 이미 파일이 있으면 그대로 둔다)
2. 가상환경 생성 안내와 `requirements.txt` 작성
3. `.gitignore` 작성 (`data/`, `.venv/`, `__pycache__/`, `.ipynb_checkpoints/`, `.env`)
4. `README.md`에 프로젝트 한 줄 설명과 실행 방법만 간단히
5. `docs/interfaces.md`는 빈 틀(섹션 제목만)으로 생성

## 작업 2: 데이터 EDA

`data/raw/`의 파일들을 대상으로 `src/eda/`에 스크립트를 만들어 실행하고, 결과를 `reports/eda.md`로 정리한다. 원본은 수정하지 않는다.

1. **파일 개요**: 파일별 행 수, 컬럼 목록과 타입, 컬럼별 결측 비율, 성공한 인코딩, 파일 크기
2. **업종 컬럼**: 상가정보는 대분류 "음식"만 필터해서 중·소분류 고유값과 빈도. 인허가 데이터는 업태구분명(또는 그에 해당하는 컬럼)의 고유값과 빈도
3. **공통 키 후보**: 각 파일에 행정동 코드, 법정동 코드, 도로명·지번 주소, 좌표 컬럼이 있는지와 그 형식(자릿수, 예시값). 파일끼리 바로 조인 가능한 키가 있는지 결론
4. **개·폐업 이력**: 인허가 데이터의 인허가일자·폐업일자 최소/최대 연도, 연도별 개업·폐업 건수, 영업상태 값 분포, 폐업일자 결측 비율
5. **좌표 검증**: 인허가 데이터 좌표값 범위. EPSG:5174로 가정하고 샘플 10개를 위경도(EPSG:4326)로 변환해 주소와 위치가 맞는지 확인. 맞지 않으면 EPSG:2097 등 다른 후보도 시도하고 결과를 기록
6. **상가정보 ↔ 인허가 대응**: 같은 행정구역(예: 시군구 하나)에서 상가정보 음식점 수와 인허가 영업 중 점포 수를 비교해 규모 차이를 기록

`reports/eda.md` 마지막에는 **"다음 단계 판단에 필요한 사항"** 섹션을 두고 다음을 정리한다.
- 파일 간 조인 방법 (코드 조인 가능 여부 / 공간 조인 필요 여부)
- 업종 공통 분류(한식·중식·일식·양식·주점·카페 등)를 만들 때 문제가 될 만한 값
- 확인 필요로 남긴 항목 목록
