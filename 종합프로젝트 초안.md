# 남음 (외식 창업 기획 도우미)

## 프로젝트 개요

아무것도 정하지 않은 예비 외식 창업자에게 **업종 선택 → 입지 추천 → 컨셉·메뉴 → 브랜드명·로고**까지 한 번에 설계해주고, 개업 이후에도 **상권 변화 알림·브랜드 자산 생성**으로 계속 쓰이는 SaaS 웹 서비스.
공공데이터로 상권을 분석하고, 그 결과를 근거로 LLM이 기획안과 브랜딩을 생성한다.

- 대상 지역: 전국
- 대상 업종: 음식점(술집 포함), 카페
- 형태: 대학교 종합 프로젝트. 1인 개발, 한 학기(15주) 기준
- 제품 형태: SaaS 웹앱. 로그인 → 내 프로젝트 저장 → 시간이 지나면 다시 계산

### 차별점

기존 상권분석 서비스(소진공 상권정보시스템, 오픈업, 나이스비즈맵)는 "지금 얼마나 팔리나"를 보여준다.
이 서비스는 인허가 개·폐업 이력으로 **"여기서 이 업종이 몇 년 안에 얼마나 폐업했나"(생존률)**를 계산하고, 그 근거로 기획안까지 생성한다.
상권분석·로고 생성 등 개별 조각은 경쟁자가 있지만, 업종 선택부터 개업 이후까지 이어붙인 서비스는 확인되지 않았다.

### 파이프라인

```
사용자 조건(예산·희망 지역·선호 업종·경험)
  → 1. 입지·업종 추천 (행정동 × 업종 점수 + 생존률. 백테스트로 검증)
  → 2. 컨셉·메뉴·가격대 (LLM. 1단계 상권 지표를 프롬프트에 반영)
  → 3. 브랜드명·로고 (LLM이 SVG 직접 생성)
  → 4. 결과 화면 (지도·업종 분포도·기획안) + 사용자 프로젝트로 저장
  → 5. 개업 이후 (저장된 프로젝트를 새 시점으로 재계산 → 상권 변화 알림, 브랜드 자산 생성)
```

앞 단계 출력이 다음 단계 입력. 단계 사이 입출력은 `docs/interfaces.md`에 JSON으로 정의하고 각 단계는 그 형식만 주고받는다.
4단계에서 저장하는 **사용자 프로젝트 객체**(위치·업종·계산 시점·점수·브랜드 정체성)가 5단계의 입력이다.

### 시점 필터: 백테스트와 모니터링의 공통 기반

인허가 데이터에 인허가일자·폐업일자가 있으므로 "특정 시점 T에 영업 중이던 점포"를 날짜 필터로 재구성할 수 있다. 이 하나로 두 가지가 해결된다.

- **백테스트**: 과거 시점(예: 2022-01) 기준으로 점수를 계산하고, 그 이후 실제 폐업과 비교해 점수가 폐업을 얼마나 가려냈는지 측정
- **상권 모니터링(5단계)**: 개업 시점 점수와 현재 시점 점수의 차이를 "반경 내 동종 개업 N곳, 폐업 M곳" 형태로 알림

매달 데이터를 새로 받지 않아도 파일 하나로 시연 가능.

## 아키텍처

SaaS 구조로 설계하되 혼자 감당할 수 있는 얇은 스택으로 시작한다.
**분석·생성 로직은 화면을 모르는 파이썬 패키지(`src/`)에 두고**, FastAPI가 그것을 감싸기만 한다. 프론트를 바꿔도 로직은 바뀌지 않는다.

| 층 | 선택 | 비고 |
|---|---|---|
| 프론트 | Next.js (`web/`) | 지도 Kakao Maps JavaScript SDK, 차트 Recharts 또는 ECharts |
| 백엔드 | FastAPI (`api/`) | `src/` 파이프라인을 엔드포인트로 노출 |
| 분석·생성 로직 | Python 패키지 (`src/`) | 화면·API 독립. 함수 단위로 테스트 |
| 인증·DB | Supabase (PostgreSQL) | 회원, 사용자 프로젝트 저장 |
| 분석 데이터 | parquet (`data/processed/`) | API 시작 시 메모리에 적재 |
| LLM | Claude API | 컨셉·메뉴·브랜드명·로고 SVG·브랜드 자산 |
| 배포 | Vercel(web) + Render(api) | 무료 티어 |

외부 의존(지도 키, LLM 키, Supabase 키)은 `.env`로 빼고, 키가 없어도 목업이 뜨게 만든다.

## 현재 단계

**0단계: 프로젝트 초기 세팅 + 데이터 EDA.** 분석·모델·API·화면 코드는 아직 만들지 않는다.

## 폴더 구조

```
project/
├─ CLAUDE.md
├─ README.md
├─ requirements.txt
├─ .gitignore
├─ data/
│  ├─ raw/         # 원본 다운로드 파일. 절대 수정·삭제하지 않음
│  ├─ interim/     # 인코딩 정리·parquet 변환 등 중간 산출물
│  └─ processed/   # 행정동 × 업종 집계표 등 최종 분석용 데이터
├─ notebooks/      # 탐색용 노트북
├─ src/            # 분석·생성 로직 (화면·API 독립)
│  ├─ eda/         # EDA 스크립트
│  ├─ scoring/     # 1단계 점수·생존률·백테스트 (3주차~)
│  ├─ concept/     # 2단계 컨셉·메뉴·가격대 (6주차~)
│  ├─ branding/    # 3단계 브랜드명·로고 SVG (6주차~)
│  └─ monitor/     # 5단계 시점 비교·브랜드 자산 (10주차~)
├─ api/            # FastAPI (8주차~)
├─ web/            # Next.js (8주차~)
├─ reports/        # eda.md, backtest.md 등 분석 결과 문서
└─ docs/           # interfaces.md 등 설계 문서
```

`src/scoring/` 이하와 `api/`, `web/`은 해당 주차에 생성한다. 지금은 만들지 않는다.

## 데이터

모두 공공데이터포털(data.go.kr) 파일데이터. 사용자가 직접 받아 `data/raw/`에 넣는다.

| 파일 | 용도 | 비고 |
|---|---|---|
| `소상공인시장진흥공단_상가(상권)정보_20260630/` (시도별 CSV 16개) | 점포 위치·업종 분포·동종 밀집도 | 영업 중인 점포만 포함(폐업 분석 불가). 업종 대10·중75·소247. 위경도·행정동코드·건물관리번호·층정보 제공. UTF-8 |
| `식품_일반음식점.csv` | 한식·중식·술집 등 개업·폐업 이력 | 인허가일자·폐업일자·영업상태·월세액·보증액·소재지면적 포함. 행정동코드 없음(시군구 코드·좌표만). 좌표는 TM(EPSG:5174로 안내됨). cp949 |
| `식품_휴게음식점.csv` | 카페 개업·폐업 이력 | 위와 같은 구조로 추정. 확인 필요 |
| `행정안전부_지역별(행정동) 성별 연령별 주민등록 인구수_20260831.csv` | 배후 인구·연령 구성 | 행정동 단위. 행정기관코드 제공 |

아직 없는 것: 행정동 경계 파일(공간 조인이 필요하다고 판단되면 그때 받음), 매출·유동인구·임대 시세(전국 원천 파일 확보 불확실 → 보조 지표로만 고려).

## 일정 (15주)

| 주차 | 할 일 | 산출물 |
|---|---|---|
| 1~2 | EDA (작업 2) · **완료** | `reports/eda.md` |
| 3~5 | 1단계: 업종 매핑 → 시군구×업종 생존표 → 검증기 → 백테스트 · **완료(3주 차)** | `src/scoring/`, `reports/categories.md`, `survival.md`, `backtest.md` |
| 6~7 | 2·3단계: 컨셉·메뉴·브랜드명·로고 SVG ← **다음** | `src/concept/`, `src/branding/` |
| 8~9 | 지도·업종 분포도, 화면 확장 (FastAPI·Next.js는 3주 차에 앞당겨 완료) | 결과 화면 |
| 10~12 | Supabase 로그인·프로젝트 저장. 5단계 시점 비교 알림·브랜드 자산 생성 | 저장된 프로젝트 재계산 |
| 13~15 | 검증 정리, 문서, 발표 준비 | 최종 보고서 |

원칙: 1~4단계를 얇게라도 먼저 관통(9주차)하고, 그 뒤에 1단계 깊이와 5단계를 더한다.

## 3주 차까지의 결과

검증기(주소·업태 → 합격/주의/위험)가 끝에서 끝까지 동작한다.

- 코호트: 2015-01~2023-06 개업 783,575곳. 시군구 × 업종 2,877칸
- 백테스트: 2015~2019로 표를 만들어 2020~2022 개업 286,800곳에 적용
- **등급별 실제 3년 생존: 합격 70.1%, 주의 63.1%, 위험 59.6%** (격차 10.6%p)
- AUC: 전체평균 0.500 → 업종만 0.546 → 업종+지역 0.570
- 업태 코드 폐지 문제를 발견해 매 실행마다 자동 감사한다 (`reports/categories.md`)

자세한 내용은 `reports/backtest.md`, 결정 이유는 `docs/decisions.md`.

## 로드맵 (이번 범위 밖)

- **폐업 위험 진단**: 5단계 상권 변화에 사용자 매출 입력을 합쳐 생존 확률을 갱신하고 메뉴·가격 조정을 제안. 매출 입력이 이탈 지점이라 보류
- **건물 단위 추천**: 상가정보의 건물관리번호·층정보로 "동종이 몰린 건물"까지는 가능. 공실·매물 데이터가 없어 "이 건물에 들어가라"는 불가
- **사업화 경로**: 예비 창업자 B2C 유료화는 일회성 이용·낮은 지불 의사로 어려움. 상가 매물 플랫폼 부가 기능, 프랜차이즈 후보지 스크리닝, 지자체 창업지원 도구가 현실적

## 작업 규칙

- 환경은 **Windows**. 경로는 `pathlib` 사용, 파일 입출력은 인코딩을 항상 명시(`utf-8` 또는 `cp949`). 콘솔 출력 한글 깨짐에 주의
- `data/raw/`는 읽기 전용으로 취급한다
- `data/` 전체는 `.gitignore`에 넣는다 (대용량·재다운로드 가능)
- 대용량 CSV는 필요한 컬럼만 `usecols`로 읽거나 `chunksize`로 처리. 한 번 읽은 원본은 `data/interim/`에 parquet로 저장해 재사용
- 인코딩을 모르면 `cp949` → `utf-8-sig` → `utf-8` 순으로 시도하고, 성공한 인코딩을 보고서에 기록
- 추측으로 채우지 말 것. 컬럼 의미·좌표계·코드 체계가 불확실하면 보고서에 "확인 필요"로 남긴다
- 스크립트는 다시 실행해도 같은 결과가 나오게 작성
- `src/`는 화면·API를 모른다. FastAPI·Next.js 관련 코드를 `src/`에 두지 않는다

## 사용 스택

- 분석: Python 3.11+, pandas, pyarrow, pyproj, openpyxl, chardet. (geopandas는 공간 조인 단계에서 추가)
- 8주차부터: FastAPI, Next.js, Supabase, Kakao Maps JavaScript SDK, Claude API

---

## 작업 1: 초기 세팅 (완료)

1. 위 폴더 구조 중 `data/`, `notebooks/`, `src/eda/`, `reports/`, `docs/` 생성
2. 가상환경 생성 안내와 `requirements.txt` 작성
3. `.gitignore` 작성 (`data/`, `.venv/`, `__pycache__/`, `.ipynb_checkpoints/`, `.env`)
4. `README.md`에 프로젝트 한 줄 설명과 실행 방법만 간단히
5. `docs/interfaces.md`는 빈 틀(섹션 제목만)으로 생성
6. GitHub 원격 `kts6450/Groundwork` 연결, 첫 커밋

## 작업 2: 데이터 EDA

`data/raw/`의 파일들을 대상으로 `src/eda/`에 스크립트를 만들어 실행하고, 결과를 `reports/eda.md`로 정리한다. 원본은 수정하지 않는다.

1. **파일 개요**: 파일별 행 수, 컬럼 목록과 타입, 컬럼별 결측 비율, 성공한 인코딩, 파일 크기
2. **업종 컬럼**: 상가정보는 대분류 "음식"만 필터해서 중·소분류 고유값과 빈도. 인허가 데이터는 업태구분명(또는 그에 해당하는 컬럼)의 고유값과 빈도
3. **공통 키 후보**: 각 파일에 행정동 코드, 법정동 코드, 도로명·지번 주소, 좌표 컬럼이 있는지와 그 형식(자릿수, 예시값). 상가정보 행정동코드와 인구 데이터 행정기관코드가 바로 조인되는지 결론
4. **개·폐업 이력**: 인허가 데이터의 인허가일자·폐업일자 최소/최대 연도, 연도별 개업·폐업 건수, 영업상태 값 분포, 폐업일자 결측 비율
5. **좌표 검증**: 인허가 데이터 좌표값 범위. EPSG:5174로 가정하고 샘플 10개를 위경도(EPSG:4326)로 변환해 주소와 위치가 맞는지 확인. 맞지 않으면 EPSG:2097 등 다른 후보도 시도하고 결과를 기록
6. **상가정보 ↔ 인허가 대응**: 같은 행정구역(예: 시군구 하나)에서 상가정보 음식점 수와 인허가 영업 중 점포 수를 비교해 규모 차이를 기록
7. **비용 관련 컬럼**: 인허가 데이터의 월세액·보증액·소재지면적 결측 비율과 값 분포. 사용자 입력 "예산"을 실제로 쓸 수 있는지 판단
8. **시점 필터 가능성**: 인허가일자·폐업일자 형식과 결측. "특정 날짜에 영업 중" 필터가 성립하는지, 폐업 상태인데 폐업일자가 없는 점포가 얼마나 되는지
9. **건물 단위 집계**: 상가정보의 건물관리번호 결측 비율과, 한 건물에 음식점이 몇 개까지 모이는지 분포

`reports/eda.md` 마지막에는 **"다음 단계 판단에 필요한 사항"** 섹션을 두고 다음을 정리한다.
- 파일 간 조인 방법 (코드 조인 가능 여부 / 공간 조인 필요 여부)
- 업종 공통 분류(한식·중식·일식·양식·주점·카페 등)를 만들 때 문제가 될 만한 값
- 시점 필터(백테스트·모니터링)가 성립하는지와 그 한계
- 확인 필요로 남긴 항목 목록
