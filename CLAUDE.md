# 남음 — 외식 창업 기획 도우미

아무것도 정하지 않은 예비 외식 창업자에게 **업종 선택 → 입지 추천 → 컨셉·메뉴 → 브랜드명·로고**까지 한 번에 설계해주고, 개업 이후에도 **상권 변화 알림·브랜드 자산 생성**으로 계속 쓰이는 SaaS 웹 서비스.
공공데이터로 상권을 분석하고, 그 결과를 근거로 LLM이 기획안과 브랜딩을 생성한다.

- 대상 지역: 전국
- 대상 업종: 음식점(술집 포함), 카페
- 형태: 대학교 종합 프로젝트. 1인 개발, 한 학기(15주)
- 제품명: **남음**. 매출이 아니라 "이 자리에서 이 업종이 얼마나 남았나"가 메시지
- 차별점: 인허가 개·폐업 이력 기반 **생존률**. 기존 상권분석은 "지금 얼마나 팔리나", 우리는 "여기서 이 업종이 얼마나 살아남았나"
- 원본 기획 문서: `종합프로젝트 초안.md` (아키텍처·일정·로드맵·EDA 항목은 그 문서가 기준)
- 결정과 이유: `docs/decisions.md`. 경쟁 서비스·사업화 검토: `docs/market.md`

## 파이프라인 (앞 단계 출력이 다음 단계 입력)

```
사용자 조건(예산·희망 지역·선호 업종·경험)
  → 1. 입지·업종 추천 (행정동 × 업종 점수 + 생존률. 백테스트로 검증)
  → 2. 컨셉·메뉴·가격대 (LLM)
  → 3. 브랜드명·로고 (LLM이 SVG 직접 생성)
  → 4. 결과 화면 (지도·업종 분포도·기획안) + 사용자 프로젝트로 저장
  → 5. 개업 이후 (저장된 프로젝트를 새 시점으로 재계산 → 상권 변화 알림, 브랜드 자산 생성)
```

단계 사이의 입출력은 `docs/interfaces.md`에 JSON 형식으로 정의하고, 각 단계는 그 형식만 주고받는다.
인허가일자·폐업일자로 "시점 T에 영업 중인 점포"를 재구성하는 **시점 필터**가 백테스트와 5단계 모니터링의 공통 기반이다.

## 아키텍처

| 층 | 선택 | 시작 시점 |
|---|---|---|
| 분석·생성 로직 | Python 패키지 `src/` (화면·API 독립) | 진행 중 |
| 백엔드 | FastAPI `api/` | 3주차 (화면용으로 당김) |
| 프론트 | Next.js `web/` (지도는 나중에) | 3주차 |
| 인증·DB | Supabase | 아직 |
| LLM | Claude API (`claude-opus-5`) | 2단계부터 |

`src/`는 FastAPI·Next.js를 모른다. 로직은 화면 없이 함수로 돌아가야 한다.

## 현재 단계

**3주 차. 검증기(주소·업태 → 합격/주의/위험)가 끝에서 끝까지 동작한다.**

- 완료: EDA, 생존표, 백테스트, 검증기, 2단계 컨셉·메뉴, 3단계 브랜드명·로고, 5단계 상권 변화, FastAPI, Next.js 화면
- 프로젝트 저장은 SQLite다. Supabase로 옮길 때 `src/projects/store.py`의 함수 시그니처만 지키면 된다
- 백테스트 결과: 합격 등급 3년 생존 70.1%, 위험 59.6%. 격차 10.6%p (2020~2022 개업 286,800곳)
- **다음 할 일: 사람 손이 필요한 것들만 남았다.** `.env`에 ANTHROPIC_API_KEY(LLM 경로 실제 확인),
  Kakao 지도 키(지도·분포도), Supabase(로그인·다기기 저장), 행정동 경계 파일(동 단위 생존표)

### 실행

```powershell
# 데이터 파이프라인 (원본 → parquet → 생존표 → 백테스트)
python -m src.eda            # 처음 한 번
python -m src.scoring.run_survival
python -m src.scoring.run_backtest
python -m src.monitor.run_timeline   # 5단계 개·폐업 집계

# 서버 (터미널 두 개)
python -m uvicorn api.main:app --port 8000
cd web; npm run dev          # http://localhost:3000
```

웹은 `/backend/*`를 `127.0.0.1:8000`으로 프록시한다(`web/next.config.ts`). API 포트를 바꾸면 그 파일도 바꾼다.

## 폴더 구조

```
.
├─ CLAUDE.md
├─ README.md
├─ requirements.txt
├─ .gitignore
├─ data/
│  ├─ raw/         # 원본 다운로드 파일. 절대 수정·삭제하지 않음
│  ├─ interim/     # 인코딩 정리·parquet 변환 등 중간 산출물
│  └─ processed/   # 행정동 × 업종 집계표 등 최종 분석용 데이터
├─ notebooks/      # 탐색용 노트북
├─ src/            # 분석·생성 로직 (화면·API 독립)
│  ├─ eda/         # EDA 스크립트
│  ├─ scoring/     # 1단계 업종 매핑·생존표·백테스트·검증기
│  ├─ concept/     # 2단계 컨셉·메뉴 (LLM, 키 없으면 목업)
│  ├─ branding/    # 3단계 브랜드명·팔레트·로고 SVG
│  ├─ monitor/     # 5단계 상권 변화·브랜드 자산
│  └─ projects/    # 사용자 프로젝트 저장 (SQLite, Supabase 자리)
├─ api/            # FastAPI. /catalog, /verify
├─ web/            # Next.js 검증 화면 (남음)
├─ reports/        # eda.md, categories.md, survival.md, backtest.md
└─ docs/           # interfaces.md 등 설계 문서
```

## 데이터 (`data/raw/`)

모두 공공데이터포털(data.go.kr) 파일데이터. 사용자가 직접 받아 `data/raw/`에 넣는다.

| 파일 | 용도 | 비고 |
|---|---|---|
| `소상공인시장진흥공단_상가(상권)정보_20260630/` (시도별 CSV 16개) | 점포 위치·업종 분포·동종 밀집도 | 영업 중인 점포만 포함(폐업 분석 불가). 업종 대10·중75·소247. 위경도·행정동코드·건물관리번호·층정보 제공. UTF-8 |
| `식품_일반음식점.csv` | 한식·중식·술집 등 개업·폐업 이력 | 인허가일자·폐업일자·영업상태·월세액·보증액·소재지면적 포함. 행정동코드 없음. 좌표는 TM(EPSG:5174로 안내됨). cp949 |
| `식품_휴게음식점.csv` | 카페 개업·폐업 이력 | 위와 같은 구조로 추정. 확인 필요 |
| `행정안전부_지역별(행정동) 성별 연령별 주민등록 인구수_20260831.csv` | 배후 인구·연령 구성 | 행정동 단위. 행정기관코드 제공 |

아직 없는 것: 행정동 경계 파일(공간 조인이 필요하다고 판단되면 그때 받음), 매출·유동인구·임대 시세(전국 원천 파일 확보 불확실 → 보조 지표로만 고려).

## 작업 규칙

- 환경은 **Windows**. 경로는 `pathlib` 사용, 파일 입출력은 인코딩을 항상 명시(`utf-8` 또는 `cp949`). 콘솔 출력 한글 깨짐에 주의
- `data/raw/`는 읽기 전용으로 취급한다
- `data/` 전체는 `.gitignore`에 넣는다 (대용량·재다운로드 가능). 폴더 구조용 `.gitkeep`만 추적
- 대용량 CSV는 필요한 컬럼만 `usecols`로 읽거나 `chunksize`로 처리. 한 번 읽은 원본은 `data/interim/`에 parquet로 저장해 재사용
- 인코딩을 모르면 `cp949` → `utf-8-sig` → `utf-8` 순으로 시도하고, 성공한 인코딩을 보고서에 기록
- 추측으로 채우지 말 것. 컬럼 의미·좌표계·코드 체계가 불확실하면 보고서에 "확인 필요"로 남긴다
- 스크립트는 다시 실행해도 같은 결과가 나오게 작성
- `src/`에 FastAPI·Next.js 관련 코드를 두지 않는다
- 외부 키(지도·LLM·Supabase)는 `.env`로 빼고, 키가 없어도 목업이 뜨게 만든다
- LLM이 만든 결과라도 **근거 숫자는 코드가 채운다**. 모델이 통계를 지어내면 이 서비스의 차별점이 무너진다
- LLM이 만든 SVG는 화면에 넣기 전에 `src/branding/svg.py`의 `sanitize_svg`를 반드시 통과시킨다
- 업종 매핑을 바꾸면 `run_survival` → `run_backtest` 순서로 다시 돌린다. 백테스트 근거 JSON을 검증기가 읽는다
- 인허가 업태 코드는 중간에 폐지된다. 새 묶음을 만들기 전에 `reports/categories.md`의 '업태 코드 사용 시기'를 확인한다

## 사용 스택

- 분석: Python 3.11+, pandas, pyarrow, pyproj, openpyxl, chardet. (geopandas는 공간 조인 단계에서 추가)
- 8주차부터: FastAPI, Next.js, Supabase, Kakao Maps JavaScript SDK, Claude API

실행: `.venv` 활성화 후 `python -m src.eda.<스크립트명>` (프로젝트 루트에서).
