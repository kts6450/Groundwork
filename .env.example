# 이 파일을 .env로 복사해 값을 채운다. .env는 git에 올라가지 않는다.

# 2·3단계 컨셉·메뉴·브랜딩 생성용. 없으면 규칙 기반 목업으로 동작한다.
# https://console.anthropic.com 에서 발급
ANTHROPIC_API_KEY=

# 아래는 아직 쓰지 않는다. 해당 단계에 들어갈 때 채운다.
# NEXT_PUBLIC_KAKAO_MAP_KEY=
# SUPABASE_URL=
# SUPABASE_ANON_KEY=
